/**
 * Application-level constants.
 *
 * Anything used in more than one module belongs here. Per-project magic
 * numbers stay close to their owners; values that are part of the public
 * API contract or cross-cutting concerns live here.
 */

export const API_PREFIX = 'api';

export const API_DEFAULT_VERSION = '1';
